[build]
  publish = "."
  functions = "netlify/functions"

[build.environment]
  NODE_VERSION = "18"

# Redirects all calls from /api/* to the functions folder
# This makes it look cleaner in your frontend code
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200